# Evidence assets for #4948 / PR #5652

Screenshots of a real Terminal.app window (captured with `screencapture -l<windowid>`)
running the reproduction live, plus the unedited daemon logs those runs produced.

- `evidence/4948/screenshots/` — terminal captures, before/after per failure
- `evidence/4948/logs/` — the daemon's own stdout logs from each run, unmodified

Hosted on a throwaway branch so binaries stay out of the PR diff. Delete with the PR.
